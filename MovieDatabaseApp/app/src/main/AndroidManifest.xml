<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pdm.isel.moviedatabaseapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MovieApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name="pdm.isel.moviedatabaseapp.ui.activity.SplashActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="pdm.isel.moviedatabaseapp.ui.activity.MovieDetailsActivity" />
        <activity android:name="pdm.isel.moviedatabaseapp.ui.activity.MovieListActivity" />
        <activity android:name="pdm.isel.moviedatabaseapp.ui.activity.ReferencesActivity" />
        <activity android:name="pdm.isel.moviedatabaseapp.ui.activity.HomeActivity" />
        <service
            android:name=".services.MovieListsJobService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />
    </application>

</manifest>